flex {filename}.l
gcc lex.yy.c -o lexer -lfl
./lexer


or 


lex {filename}.l
gcc lex.yy.c -o lexer -ll
./lexer

or 

flex {filename}.l
gcc lex.yy.c
./a.out


or 

put file name in build.sh
chmod +x build.sh (you have to do this step only the first time running this)

./build.sh
